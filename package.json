{
  "private": true,
  "cacheDirectories": [
    "frontend/node_modules"
  ],
  "engines": {
    "node": "24.x",
    "npm": "11.x"
  },
  "scripts": {
    "heroku-postbuild": "ls -la frontend/package-lock.json && cd frontend && npm install --include=dev && npm run build"
  }
}
